#include<unistd.h>
#include"main.h"
/**
 * _islower(int c) -Entry program.
 * Return: 1 if true otherwise 0
 */
int _islower(int c)
{
	int c;

	if (c = 'a';  <= 'z')
	{
		return (1);
	}
	else
	{
		return (0);
	}
}
