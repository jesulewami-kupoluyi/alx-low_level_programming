#include"main.h"
/**
 * print_last_digit -Entry program
 * @n: integar
 * Return:0 if true
 */
int print_last_digit(int n)
{
	int res = n % 10;

	if (n < o)
	{
		res = res * -1;
	}
	_putchar(res + '0');
	return (res);
}
