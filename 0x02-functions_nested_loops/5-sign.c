#include"main.h"
/**
 * print_sign -Entry program
 * @n: integar
 * Return: 1 if true otherwise 0
 */
int print_sign(int n)
{
	if (c >= 'a' && c <= 'z')
	{
		return (1);
	}
	else if (c >= 'A' && c <= 'Z')
	{
		return (1);
	}
	else
	{
		return (0);
	}
}
