#include"main.h"
/**
 * _puts_recursion -a function to print strings
 * @s: character value
 * return: positive value
 */
void _puts_recursion(char *s)
{
	char *s = &n;

	_putchar(&n);
	return (0);
}
