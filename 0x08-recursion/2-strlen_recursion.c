#include"main.h"
/**
 * _strlen_recursion - function prints string length of a given value
 * @s:character value.
 * Return: void
 */
int _strlen_recursion(char *s)
{
	int i;

	for (i = 0; s[i] != '\0'; ++i)
	{
		_putchar(n);
	}
}
